# include <stdio.h>
# include <string.h>
# include <stdlib.h>

# define ROZMIAR 50

struct wezel{


char imie[ROZMIAR];
char nazwisko[ROZMIAR];
struct wezel * lewy;
struct wezel * prawy;

}root;


void wpisywanie( struct wezel *root){

	wezel * biezacy;
	biezacy=(Wezel*) malloc (sizeof(Wezel));
	if(root==NULL)
		root=biezacy;
	else if(strcmp(biezacy->imie, root->lewy
	

	if ( root
	char wejscie[ROZMIAR];
	printf( " Podaj imie Twojego kolegi ktorego chcesz tu umiescic: \n");
	gets (wejscie);
	



}

int main(){

	struct wezel * wroot;
	wroot=&root;
	











}

